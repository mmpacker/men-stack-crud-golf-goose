// const router = require('express').Router()
// const coursesCtrl = require('../controllers/api/courses')

// router.get('/courses', isLoggedIn, coursesCtrl.index);
// router.get('/courses/:id', isLoggedIn, coursesCtrl.show);
// router.post('/courses', isLoggedIn, coursesCtrl.create);
// router.put('/courses/:id', isLoggedIn, coursesCtrl.update);
// router.delete('/courses/:id', isLoggedIn, coursesCtrl.delete)


// function isLoggedIn(req, res, next) {
//     if (req.isAuthenticated()) return next();
//     res.redirect('/auth/google');
//   }

// module.exports = router;